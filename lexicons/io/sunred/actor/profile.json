{
  "lexicon": 1,
  "id": "io.sunred.actor.profile",
  "defs": {
    "main": {
      "type": "record",
      "description": "A Sunred user profile. Stored in the user's PDS repo under io.sunred.actor.profile/self.",
      "key": "literal:self",
      "record": {
        "type": "object",
        "required": ["handle", "createdAt"],
        "properties": {
          "handle": {
            "type": "string",
            "maxLength": 64,
            "description": "The user's Sunred handle (e.g. 'fuego'). Unique within the instance."
          },
          "bio": {
            "type": "string",
            "maxLength": 500,
            "description": "Short biography."
          },
          "displayName": {
            "type": "string",
            "maxLength": 255,
            "description": "Human-readable display name."
          },
          "instanceUrl": {
            "type": "string",
            "format": "uri",
            "description": "Base URL of the Sunred instance this user belongs to."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime",
            "description": "ISO 8601 timestamp when the profile was first created."
          }
        }
      }
    }
  }
}
